# Secrets et config locale (ne JAMAIS committer)
api/config.php
api/config-values.json
NOTE_TRANSFERT.md

# Dépendances installées côté serveur
lib/stripe/
vendor/

# Contenu généré / uploads clients
uploads/reports/*
!uploads/reports/.gitkeep

# Logs, OS, état d'outils
*.log
.DS_Store
.autodeploy-state.json

# Outils de déploiement IONOS (obsolètes avec git)
autodeploy.py
deploy.py
pull.py
