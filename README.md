# ABYS AI
Plateforme d'audit IA pour PME/TPE (abys.ai). PHP 8.x, MySQL, Stripe, SMTP Brevo.
Copier api/config.example.php vers api/config.php (jamais versionné). `composer install` pour le SDK Stripe.
